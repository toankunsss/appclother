import { StyleSheet, Text, View } from "react-native";
import React from "react";
import CheckPayment from "@/app/(screen)/checkPayment";
const search = () => {
  return (
    <View style={{ flex: 1 }}>
      <CheckPayment />
    </View>
  );
};

export default search;

const styles = StyleSheet.create({});
